PORT=4500
MONGO_URI=mongodb://mongo:27017/agriplan
NODE_ENV=production

# Allow requests from your Netlify frontend.
# Set to the exact Netlify URL (no trailing slash).
CORS_ORIGIN=https://<YOUR-NETLIFY-APP>.netlify.app
