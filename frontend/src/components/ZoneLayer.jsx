// ZoneLayer.jsx — replaced by imperative Leaflet rendering in CanvasMap.jsx
// Kept as a stub so any remaining imports during transition don't break.
export default function ZoneLayer() {
  return null;
}
