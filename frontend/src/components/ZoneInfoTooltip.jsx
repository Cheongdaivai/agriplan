// ZoneInfoTooltip.jsx — HTML replacement for the old Konva tooltip.
// Zone hover info is now handled by Leaflet's bindTooltip() in CanvasMap.jsx.
// This stub exists so any legacy import references don't break.
export default function ZoneInfoTooltip() {
  return null;
}
