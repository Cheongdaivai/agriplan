// useCanvasInteraction.js — replaced by imperative Leaflet event handling in CanvasMap.jsx.
// All map click / dblclick logic now lives in the MapController component.
// This stub is kept so any lingering imports don't break during transition.

/**
 * @deprecated No-op stub. Use MapController in CanvasMap.jsx instead.
 */
export function useCanvasInteraction() {
  return {
    handleStageClick: () => {},
    handleStageDblClick: () => {},
  };
}
